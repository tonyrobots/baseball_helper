<%= form_with(model: pitcher, local: true) do |form| %>
  <% if pitcher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pitcher.errors.count, "error") %> prohibited this pitcher from being saved:</h2>

      <ul>
      <% pitcher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
